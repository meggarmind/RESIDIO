---
trigger: always_on
---

# Self-correction

Superseded by `CORE.md` -- read `CORE.md` section 13 ("Verification protocol"), which carries
the full checklist: lint before finishing, Supabase join ambiguity (`PGRST201`), payload
symmetry between create/update and get, and client state sync after `router.refresh()`.
